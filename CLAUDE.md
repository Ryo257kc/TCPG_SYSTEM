# このプロジェクトで最初に読むもの

このファイルはツールが毎回自動で読み込む入口。詳しいルールはここには書かない。

## 必読

- どの画面・機能を触る前も、まず `docs/rules/00_global.md` を読む。
- 次に対象ページの `docs/rules/{domain}/` フォルダを読む（`README.md` に読む順番あり）。
- 計算ロジックを触る前は、対象ドメインの `10_calculation_basis.md` の「実装:」欄で
  対応コードを確認する。
- コードの配置・CSS・ルーティング等の実務ルールは `docs/rules/01_code_structure.md`。
- 会話が長くなり要約（コンテキスト圧縮）が入った後も、この指示は変わらず有効。
  一度ルールを読んだからといって次のタスクで省略しない。
